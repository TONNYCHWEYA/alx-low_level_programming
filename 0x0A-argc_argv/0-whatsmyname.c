#include <stdio.h>
#include <stdlib.h>
/**
 * main -print the name of program
 * @argc: number of counts
 * @argv: points to the array of string
 * Return: nothing
 */
int main(int argc, char *argv[])
{
printf("%s\n", argv[0]);
exit(EXIT_SUCCESS);
}
