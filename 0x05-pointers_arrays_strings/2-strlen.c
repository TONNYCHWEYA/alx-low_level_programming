#include "main.h"
/**
 * int _strlen - return length of a string
 * @s: print i char s
 * Return: length of a string
 */
int _strlen(char *s)
{
int len;
int u = 0;
while (s[len] != 0)
{
u++;
}
return (u);
}
